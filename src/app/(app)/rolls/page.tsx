import { PagePlaceholder } from "@/components/page-placeholder";

export default function RollsPage() {
  return (
    <PagePlaceholder
      title="Rolls"
      description="The roll library: every roll shown chronologically with its generated title, film stock, camera, frame count, date range, status, and whether photos have been synced from Google Drive."
      phase="Placeholder for now. Built in Phase 2 (roll creation, roll library) and Phase 4 (review)."
    />
  );
}
