import { PagePlaceholder } from "@/components/page-placeholder";

export default function GearPage() {
  return (
    <PagePlaceholder
      title="Gear"
      description="Cameras and lenses. A camera's available shutter speeds and a lens's available aperture stops define exactly which exposure values the active roll may present while shooting."
      phase="Placeholder for now. Built in Phase 1/2 (gear management)."
    />
  );
}
