import { PagePlaceholder } from "@/components/page-placeholder";

export default function ActiveRollPage() {
  return (
    <PagePlaceholder
      title="Active Roll"
      description="The dedicated shooting interface — the highest-priority experience in the app. While shooting, this screen shows the current frame number, exposure controls, and the NEXT FRAME action, with forward-only setting inheritance between frames."
      phase="Placeholder for now. Built in Phase 3 (active roll, frame advancement, state inheritance)."
    />
  );
}
