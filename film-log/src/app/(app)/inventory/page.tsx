import { PagePlaceholder } from "@/components/page-placeholder";

export default function InventoryPage() {
  return (
    <PagePlaceholder
      title="Inventory"
      description="Unused film stocks and quantities: film stock, ISO, format, quantity, and expiration date. Creating a roll consumes one unit of the selected film stock."
      phase="Placeholder for now. Built in Phase 2 (film inventory)."
    />
  );
}
